package com.vastu.realestate.appModule.rateUs

import android.app.Application
import androidx.lifecycle.AndroidViewModel

class RateUsViewModel(application: Application) : AndroidViewModel(application) {
}